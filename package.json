{
  "name": "vpn-remna-miniapp",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "api": "cd backend && .venv\\Scripts\\uvicorn app.main:app --reload --reload-dir app --port 8000",
    "bot": "cd backend && .venv\\Scripts\\python -m bot.main",
    "api:setup": "cd backend && python -m venv .venv && .venv\\Scripts\\pip install -r requirements.txt"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-router-dom": "^6.20.0"
  },
  "devDependencies": {
    "@types/react": "^18.2.43",
    "@types/react-dom": "^18.2.17",
    "@vitejs/plugin-react": "^4.2.1",
    "vite": "^5.0.8"
  }
}
